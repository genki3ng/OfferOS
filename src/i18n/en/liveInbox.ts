const liveInbox = {
  badgeNew: "new",
  empty: "All clear ✅ (📨 hand-offs and browser-extension captures land here; the next Claude session handles them on start-up)",
  live: "↻ Live (reads the repo directly — newly handed-off items show up instantly)",
  snapshot: "Build-time snapshot — newly handed-off items appear after a ~1 minute rebuild; add a token to make this card live",
};

export default liveInbox;
