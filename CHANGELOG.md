# 更新日志 / Changelog

OfferOS 的重要变更都记在这里。版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
**你当前的版本显示在站点页脚**；如何把上游更新拉到你已 clone 的仓库，见 [README「保持更新」](README.md#-保持更新跟进上游升级)。

> **版本节奏**：每累积**约 10 条更新**（≈10 个 commit）就 bump 一次 minor（+0.1，如 0.2 → 0.3），在此新开一节归纳这批更新、`package.json` 同步改版本。

## [未发布 Unreleased]

_累积中，约 10 条更新后切下一个 minor。_

### 新增 Added
- **跟进雷达（Follow-up radar）**：首页新增一张卡，把每家「已投 / 已内推 / 面完」后**沉默了几天**实时算出来（浏览器端算，不吃构建时间、不会过期），按 SLA 标 🔴 该催 / 🟡 快到线 / 🟢 还在等，红条直接给建议动作。SLA 分类可调（内推 14 天 · 自投 21 天 · 面后 7 天 · LinkedIn 连接 7 天，改 `src/lib/followup.ts`）；数据来自 `data/tracker.json` 每家的 `lastContact` + `awaiting` 两个新字段（都可选，不填就不进雷达）。
- **文档阅读体验大改**：`/docs` 等处把 markdown 从「全宽文字墙」改成可读文档——① 阅读列宽（正文 ~42rem 左对齐，表格/代码/图片破格 60rem，`/docs` 居中成文档列）；② `> ` 引用按行首 emoji 自动上色成**语义 callout**（🔴 风险 / 🟢 已搞定 / 🟡 待办 / 📌 重点，无 emoji 回落中性）；③ **长段落按句末标点 `。！？` 自动断成「一句一行」**（两级间距：段落 1.5em > 句内 0.95em，不再糊成一团）；④ 标题层级 + 表格斑马纹 + 列表珊瑚 marker + 任务清单去 bullet。
- 练习台**语言题（产品 Sense / 统计 / 行为面）也支持先口述作答再对照**：题目下方直接给输入框（适合语音转文字，附常见 App 推荐 superwhisper / Wispr Flow / MacWhisper / 系统自带），「显示要点」后「你的答案 / 参考要点」上下对照——和编程题一致的练法。

### 修复 Fixed
- **全站长文折叠**：新通用组件 `ClampHtml`——岗位库「岗位与说明」列重构为**标题行 + 折叠说明**（标题/地点不再和长说明糊在一格），pipeline「下一步」、内推表全部备注格、公司页「下一步」统一超行折叠 + 「展开/收起」；顺带修 pipeline 卡角色行显示原始 `**` 星号、PERM/内推长 pill 被 inline-flex 排成多列的乱版。
- **日志长句结构化**：新 `LogSteps` 组件——「下一步」等流水句按句子拆成**带状态图标的步骤清单**（一步一行、超限折 +N 条）；新 `NoteField`——PERM/内推字段短语走彩色 pill、长句走同色系**便签块**；内推渠道表公司列「主名（括注）」拆两行；表格里 pill 允许折行 + 长文格保底列宽，根治长 pill 撑爆列宽、窄列竖绕爆行高。
- **事件标题不再把长文原样灌进卡片**：关键日期 / 「下一步」写成日志长句时，首页 hero / 「紧随其后」/ 本周事件、时间线、公司卡的事件标题一律自动摘要成一行事件名（剥日期星期时间、砍长括注、只留第一句、上限 64 字；hover 看全文），并全部加多行钳制兜底；顺带修了手机上点按「紧随其后」卡整块出现下划线的问题。契约见 STYLEGUIDE「关键日期」段。

## [0.3.0] — 2026-06-28

> 自 0.2.0 起累积约 10 条更新，按版本节奏 bump。

### 新增 Added
- **练习台内联显示批改点评**：交给 Claude 批改后的点评（写在 practice-log 的「备注」列）现在直接挂在题目卡里——问题文下方一个可折叠的「📝 Claude 点评」框（点开看时间 · 自评 · 点评正文），题表里有点评的题加 📝 徽章。不用再去翻整张 practice-log 表。
- **练习台编程题自由 coding 框**：SQL / Python 等编程题先在等宽暗色框里手写解法（模拟实战），再「显示要点」与参考解法**上下对照**差异（Tab 在框内缩进、不跳焦点）；一键把解法交 Claude 批改。
- **今日卡 / 时间线显示面试的具体时间**（不再只有日期）。

### 修复 Fixed
- **文本选中在深色 code 框 / 浅底色块里看不清**：`::selection` 解耦背景与字色（浅底深字 / 深底浅字），选中内容始终可读、也看得出选中范围。

### 改进 Changed
- **题库质量契约**：STYLEGUIDE 新增「**题面必须具体可作答**」——出题不能只给话题标签，要写清算什么 / 怎么过滤分组 / 输出哪些列 + schema。
- **去标识化铁律细化**：题库去痕**仅针对公开模板**（不点名公司、`npm run check` 模板态硬拦）；用户 fork 配置成私库后题库随你、`check` 自动放行。

## [0.2.0] — 2026-06-27

### 修复 Fixed
- **文档页 frontmatter 不再被当成大标题**。带 YAML frontmatter（`company / round / date / …`）的速备包等文档，过去整段 `---…---` 被 markdown 解析成 setext 大标题、糊成一长行当标题。现在 frontmatter 被解析并剥离，渲染成顶部一排干净的**信息条**（图标 + 值），正文从真正的 `#` 标题开始。
- **时间线「即将发生 / Deadline」不再把历史里程碑刷成「逾期」**。过去公司「关键日期」表里**发生过**的事（如「内推发出 06-12」）会以「逾期 N 天」红条堆进时间线。现在只有**有意安排却滑过**的待办（tracker `⏰MM-DD` 下一步、offer 截止日）才算逾期；历史里程碑归「一路走来」。
- **时间线「投递待办」按真实阶段判定**。过去靠单岗 `📮` 标记判「待投」，该标记常年没人维护 → 已投递 / 面试中的岗位被误显成「待投」。现在按公司 **tracker 状态**推断阶段（待投 / 已投·等回复 / 面试中），状态下拉一改即同步。

### 新增 Added
- 站点**页脚显示当前版本号**，点开 = 本更新日志（`/docs/CHANGELOG`）。
- 速备包 frontmatter 新增可选字段约定 `interviewers`（顶部信息条展示当天面试阵容）。
- README 新增「**保持更新（跟进上游升级）**」一节，教已 clone 的用户怎么拉上游改进。

## [0.1.0]

- 首个公开模板版本：**markdown 即数据库** + Next.js 仪表盘（今日 / 公司 / pipeline / 备战 / 练习台 / Offers / 时间线 / Comp / 文档）。
- 多角色备战 packs（DS / DE / SWE / PM / ML），自带约 150 题、每题带解法。
- `/onboard` 向导一键个性化、`/comp` levels.fyi 风格薪资页、Web Clipper 浏览器扩展、联网情报（Agent-Reach + 抓取手册）。
- 可安装 PWA、暖光双主题、界面中英双语、浏览器直连 GitHub 的写通道（站上勾任务 / 改状态 / 派活）。
